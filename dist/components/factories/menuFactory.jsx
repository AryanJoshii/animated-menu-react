export default (function (styles) {
    if (!styles) {
        throw new Error("No styles provided!");
    }
    var ARROW_DOWN = 'ArrowDown';
    var ARROW_UP = 'ArrowUp';
    var ESCAPE = 'Escape';
    var SPACE = ' ';
    var HOME = 'Home';
    var END = 'End';
    var Menu = function (props) {
        return (<>HELLO WORLD, MY NAME IS CARLO</>);
    };
    return Menu;
});
